# Copy this content to create your .env file
# File name: .env (in the same directory as server.js)

# Email Configuration for Gmail OTP
EMAIL_USER=skportfolio57@gmail.com
EMAIL_PASS=your-gmail-app-password-here

# MongoDB Configuration (optional)
MONGODB_URI=mongodb://127.0.0.1:27017/portfolio

# Server Configuration
PORT=3000

# Session Secret (change this to a random string)
SESSION_SECRET=your-secret-key-here

# Instructions:
# 1. Create a new file named ".env" (without quotes)
# 2. Copy the content above into the .env file
# 3. Replace "your-gmail-app-password-here" with your actual Gmail App Password
# 4. To get Gmail App Password:
#    - Go to Google Account settings
#    - Security > 2-Step Verification > App passwords
#    - Generate password for "Mail" application
#    - Use that 16-character password (no spaces)
# 5. Save the .env file
# 6. Restart the server: node server.js
